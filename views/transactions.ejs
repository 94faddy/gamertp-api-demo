<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Casino Wallet</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            
        }
        .page-header {
            background: white;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        .stat-card {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            text-align: center;
        }
        .stat-icon {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            margin: 0 auto 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
        }
        .stat-icon.bets {
            background: #fff3cd;
            color: #ffc107;
        }
        .stat-icon.wins {
            background: #d4edda;
            color: #28a745;
        }
        .stat-icon.rate {
            background: #d1ecf1;
            color: #17a2b8;
        }
        .stat-icon.profit {
            background: #f8d7da;
            color: #dc3545;
        }
        .stat-value {
            font-size: 2rem;
            font-weight: bold;
            color: #333;
        }
        .stat-label {
            color: #666;
            font-size: 0.9rem;
            margin-top: 5px;
        }
        .filter-card {
            background: white;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 30px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
        }
        .transactions-card {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
        }
        .transaction-row {
            border-bottom: 1px solid #f0f0f0;
            padding: 20px 0;
            transition: all 0.3s;
        }
        .transaction-row:last-child {
            border-bottom: none;
        }
        .transaction-row:hover {
            background: #f8f9fa;
            margin: 0 -15px;
            padding: 20px 15px;
            border-radius: 10px;
        }
        .transaction-icon {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
        }
        .transaction-icon.win {
            background: #d4edda;
            color: #28a745;
        }
        .transaction-icon.bet {
            background: #fff3cd;
            color: #ffc107;
        }
        .badge-win {
            background: #d4edda;
            color: #28a745;
            padding: 5px 10px;
            border-radius: 5px;
            font-size: 0.85rem;
        }
        .badge-bet {
            background: #fff3cd;
            color: #ffc107;
            padding: 5px 10px;
            border-radius: 5px;
            font-size: 0.85rem;
        }
        .no-transactions {
            text-align: center;
            padding: 60px 20px;
            color: #999;
        }
        .no-transactions i {
            font-size: 4rem;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar', { active: 'transactions' }) %>

    <div class="container">
        <!-- Page Header -->
        <div class="page-header">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <h2 class="mb-2">
                        <i class="fas fa-history text-primary me-2"></i>
                        ประวัติการทำรายการ
                    </h2>
                    <p class="text-muted mb-0">ดูประวัติการเดิมพันและรายการทั้งหมดของคุณ</p>
                </div>
                <div class="col-md-4 text-end">
                    <div class="d-flex align-items-center justify-content-end">
                        <i class="fas fa-wallet text-primary me-2 fs-5"></i>
                        <div>
                            <small class="text-muted d-block">ยอดเงินคงเหลือ</small>
                            <strong class="text-primary">
                                <%= user.balance.toLocaleString('th-TH', {minimumFractionDigits: 2}) %> <%= user.currency %>
                            </strong>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Statistics -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon bets">
                    <i class="fas fa-dice"></i>
                </div>
                <div class="stat-value"><%= stats.totalBets %></div>
                <div class="stat-label">จำนวนเดิมพัน</div>
                <small class="text-muted">
                    <%= stats.totalBetAmount.toLocaleString('th-TH', {minimumFractionDigits: 2}) %> <%= user.currency %>
                </small>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon wins">
                    <i class="fas fa-trophy"></i>
                </div>
                <div class="stat-value"><%= stats.totalWins %></div>
                <div class="stat-label">จำนวนชนะ</div>
                <small class="text-muted">
                    <%= stats.totalWinAmount.toLocaleString('th-TH', {minimumFractionDigits: 2}) %> <%= user.currency %>
                </small>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon rate">
                    <i class="fas fa-percentage"></i>
                </div>
                <div class="stat-value">
                    <%= stats.totalBets > 0 ? ((stats.totalWins / stats.totalBets * 100) || 0).toFixed(0) : 0 %>%
                </div>
                <div class="stat-label">อัตราชนะ</div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon profit">
                    <i class="fas fa-chart-line"></i>
                </div>
                <div class="stat-value <%= (stats.totalWinAmount - stats.totalBetAmount) >= 0 ? 'text-success' : 'text-danger' %>">
                    <%= (stats.totalWinAmount - stats.totalBetAmount).toLocaleString('th-TH', {minimumFractionDigits: 2, maximumFractionDigits: 2}) %>
                </div>
                <div class="stat-label">กำไร/ขาดทุน</div>
            </div>
        </div>

        <!-- Filters -->
        <div class="filter-card">
            <form method="GET" action="/transactions">
                <div class="row g-3">
                    <div class="col-md-3">
                        <label class="form-label">
                            <i class="fas fa-calendar-alt me-1"></i> วันที่เริ่มต้น
                        </label>
                        <input 
                            type="date" 
                            class="form-control" 
                            name="startDate" 
                            value="<%= filters.startDate %>"
                        >
                    </div>
                    <div class="col-md-3">
                        <label class="form-label">
                            <i class="fas fa-calendar-alt me-1"></i> วันที่สิ้นสุด
                        </label>
                        <input 
                            type="date" 
                            class="form-control" 
                            name="endDate" 
                            value="<%= filters.endDate %>"
                        >
                    </div>
                    <div class="col-md-3">
                        <label class="form-label">
                            <i class="fas fa-filter me-1"></i> ประเภท
                        </label>
                        <select class="form-select" name="type">
                            <option value="">ทั้งหมด</option>
                            <option value="bet" <%= filters.type === 'bet' ? 'selected' : '' %>>เดิมพัน</option>
                            <option value="win" <%= filters.type === 'win' ? 'selected' : '' %>>ชนะ</option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label">&nbsp;</label>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-search me-2"></i> ค้นหา
                            </button>
                        </div>
                    </div>
                </div>
            </form>
        </div>

        <!-- Transactions List -->
        <div class="transactions-card">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h5 class="mb-0">
                    <i class="fas fa-list me-2"></i> รายการทั้งหมด
                </h5>
                <span class="badge bg-primary"><%= transactions.length %> รายการ</span>
            </div>

            <% if (transactions && transactions.length > 0) { %>
                <% transactions.forEach(txn => { 
                    let type = '';
                    let amount = 0;
                    let description = '';
                    
                    if (txn.betAmount > 0 && txn.payoutAmount === 0) {
                        type = 'bet';
                        amount = txn.betAmount;
                        description = 'เดิมพัน';
                    } else if (txn.payoutAmount > 0) {
                        type = 'win';
                        amount = txn.payoutAmount - txn.betAmount;
                        description = amount > 0 ? 'ชนะ' : 'คืนเงิน';
                    }
                    
                    const date = new Date(txn.createdDate || txn.timestamp);
                %>
                <div class="transaction-row">
                    <div class="row align-items-center">
                        <div class="col-auto">
                            <div class="transaction-icon <%= type %>">
                                <i class="fas <%= type === 'win' ? 'fa-trophy' : 'fa-dice' %>"></i>
                            </div>
                        </div>
                        <div class="col">
                            <div class="fw-bold mb-1"><%= description %></div>
                            <small class="text-muted">
                                <i class="fas fa-gamepad me-1"></i>
                                <%= txn.gameName || txn.gameCode || '-' %>
                            </small>
                        </div>
                        <div class="col-auto text-center">
                            <div class="badge-<%= type %>">
                                <i class="fas <%= type === 'win' ? 'fa-arrow-up' : 'fa-arrow-down' %> me-1"></i>
                                <%= type === 'win' ? 'ชนะ' : 'เดิมพัน' %>
                            </div>
                            <br>
                            <small class="text-muted">
                                <%= date.toLocaleDateString('th-TH') %>
                                <br>
                                <%= date.toLocaleTimeString('th-TH', {hour: '2-digit', minute: '2-digit'}) %>
                            </small>
                        </div>
                        <div class="col-auto text-end">
                            <div class="fw-bold fs-5 <%= type === 'win' ? 'text-success' : 'text-warning' %>">
                                <%= type === 'win' ? '+' : '-' %>
                                <%= Math.abs(amount).toLocaleString('th-TH', {minimumFractionDigits: 2}) %>
                            </div>
                            <small class="text-muted"><%= user.currency %></small>
                        </div>
                    </div>
                    
                    <% if (txn.betAmount > 0 || txn.payoutAmount > 0) { %>
                    <div class="row mt-2">
                        <div class="col-12">
                            <small class="text-muted">
                                <i class="fas fa-info-circle me-1"></i>
                                เดิมพัน: <%= txn.betAmount.toLocaleString('th-TH', {minimumFractionDigits: 2}) %> | 
                                จ่ายคืน: <%= txn.payoutAmount.toLocaleString('th-TH', {minimumFractionDigits: 2}) %>
                                <% if (txn.txnId) { %>
                                | ID: <%= txn.txnId %>
                                <% } %>
                            </small>
                        </div>
                    </div>
                    <% } %>
                </div>
                <% }); %>
            <% } else { %>
                <div class="no-transactions">
                    <i class="fas fa-inbox"></i>
                    <h5>ยังไม่มีประวัติการทำรายการ</h5>
                    <p class="text-muted">เริ่มเล่นเกมเพื่อดูประวัติการทำรายการของคุณ</p>
                    <a href="/games" class="btn btn-primary">
                        <i class="fas fa-gamepad me-2"></i> เริ่มเล่นเกม
                    </a>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
